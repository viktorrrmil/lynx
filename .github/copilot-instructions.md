# Copilot instructions

When generating responses that modify code:
- Always list the changed files.
- Put them under a section called "Changed files".
- Show from which to which line the changes were made in each file.
- For each file, show the diff of the changes made.